// format.js

function formatNumber(num) {
    return num.toFixed(2).replace(/\d(?=(\d{3})+\.)/g, '$&,');
}

// Expose the function globally or export it (for modules)
window.formatNumber = formatNumber;  // Makes the function available globally
